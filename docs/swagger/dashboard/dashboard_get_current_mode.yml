get:
  summary: 현재 모드 조회
  description: 현재 설정된 제어 모드를 반환합니다.
  parameters:
    - name: type
      in: query
      required: false
      type: string
      enum: []
  responses:
    200:
      description: 현재 모드
      schema:
        type: object
        properties:
          current_mode:
            type: string
            example: light