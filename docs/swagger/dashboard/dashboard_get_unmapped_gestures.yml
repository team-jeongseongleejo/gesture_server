get:
  summary: 매핑되지 않은 손동작 목록 조회
  parameters:
    - name: mode
      in: query
      required: true
      schema:
        type: string
      description: 모드 이름
  responses:
    200:
      description: 매핑되지 않은 손동작 이름 리스트
      content:
        application/json:
          example: ["thumbs_up", "thumbs_left", "promise"]