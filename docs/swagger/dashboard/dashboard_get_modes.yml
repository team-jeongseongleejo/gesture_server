get:
  summary: 사용 가능한 모드 목록 조회
  parameters:
    - name: type
      in: query
      required: false
      type: string
      enum: []
  responses:
    200:
      description: 사용 가능한 모드 리스트
      content:
        application/json:
          example: ["light", "fan", "projector"]